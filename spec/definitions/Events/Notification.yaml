type: object
properties:
  endpoint:
    type: string
    example: 'https://www.some-endpoint.com'
  content_type:
    type: string
    example: application/json
  success:
    type: boolean
    example: false
  attempts:
    type: array
    items:
      $ref: '#/definitions/NotificationAttempt'